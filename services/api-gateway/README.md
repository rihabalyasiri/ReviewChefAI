# api-gateway (planned)

Will be the single external entry point that routes requests to `review-fetcher-service`,
`analysis-service`, and any future services, instead of clients calling each service directly.

Not implemented yet — this is a placeholder marking where that service will live once there is more than
one internal service worth fronting.
